public String nameFromEmail(String email) {
    String first = email.substringBefore(.).toLower().capitalize();
    String last = email.substringBetween(.,@).toLower().capitalize();
    return (first + last);

}